# Task Master 단축 명령어

Task Master CLI를 `tm` 단축어로 실행합니다.

## 사용법

```bash
tm <command> [options]
```

## 예시

- `tm list` - 모든 작업 목록 보기
- `tm next` - 다음 작업 보기
- `tm show 1` - 작업 1 상세 보기
- `tm set-status --id=1 --status=done` - 작업 완료 표시
